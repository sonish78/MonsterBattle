CREATE TABLE `monsters` (
  `id` integer,
  `name` text NOT NULL,
  `attack` integer NOT NULL,
  `defense` integer NOT NULL,
  `hp` integer NOT NULL,
  `speed` integer NOT NULL,
  PRIMARY KEY (`id`)
);